{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest", "--isolated", "--headless"],
      "env": {},
      "allowlist": ["http://127.0.0.1:8321", "http://localhost:8321"],
      "description": "Chrome DevTools MCP for nodary local dashboard (isolated, 127.0.0.1 only). Use --isolated; --autoConnect not chosen. See P2-2."
    }
  }
}
